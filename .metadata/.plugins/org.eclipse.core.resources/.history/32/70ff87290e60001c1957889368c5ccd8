package main;

public class Player {
	String name;
	Die[] dice;
	public Player(String _name) {
		name = _name;
		dice = new Die[5];
		for(int i=0; i<dice.length; i++) {
			dice[i] = new Die();
		}
	
	public renderDice(Graphics2D g) {
		for(Die d : dice) {
			d.render(g);
		}
	}
	}
}
